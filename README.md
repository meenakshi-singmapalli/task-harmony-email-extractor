# Task Harmony Email Extractor

A Python tool to extract shipment details from freight forwarding emails into structured JSON.

## Features
- Extract origin, destination, cargo weight, volume, and dangerous goods info
- Handles sea import/export LCL shipments
- Saves output in `output.json`
